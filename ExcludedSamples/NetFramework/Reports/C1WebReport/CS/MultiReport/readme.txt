MultiReport
------------------------------------------------------------------------
Select a report from a list and render it on your web page.

The sample builds a list of reports available in an XML file and populates
a list. The user can select any of the reports and render them on the
web page.