NumPadDropDown1
------------------------------------------------------------------------
Simple numpad control based on C1NumericEdit.

Replaces the standard calculator in a C1NumericEdit control with the
custom dropdown numpad. This numpad allows to enter digits with the
mouse