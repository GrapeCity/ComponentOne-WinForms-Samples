Blinker
------------------------------------------------------------------------
Use CellStyles to make cells blink.

The sample creates a custom style and uses a timer to change the
foreground color on that style. This causes all cells that have that
style to blink.