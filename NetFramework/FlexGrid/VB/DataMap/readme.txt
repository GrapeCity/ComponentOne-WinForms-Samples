DataMap
------------------------------------------------------------------------
Use data-mapped columns when bound to a data source.

The sample binds the grid to the Northwind Products table, and attaches
a Hashtable to the DataMap property of the CategoryID column.

This way, the grid displays the CategoryName instead of the ID. (The 
value stored in the grid is still the CategoryID).