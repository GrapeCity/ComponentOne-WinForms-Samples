AnimatedGif
-------------------------------------------------------------------
Shows how to display animated gifs in grid cells.

The .NET image class loads all the frames in animated gif files. The 
sample loads several animated gifs, then uses a timer to increment 
the current frame on each one and to invalidate grid cells that
contain animated images.