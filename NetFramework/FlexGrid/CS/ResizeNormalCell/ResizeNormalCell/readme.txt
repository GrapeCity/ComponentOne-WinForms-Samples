ResizeNormalCell
------------------------------------------------------------------------------------------------
Shows how to resize columns with the mouse without using column headers.

The sample shows a C1FlexGridResizer class that handles mouse events over regular cells
and allows users to resize columns by dragging the cell boundaries as if they were 
column headers.