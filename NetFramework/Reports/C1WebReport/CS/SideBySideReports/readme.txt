SideBySideReports
------------------------------------------------------------------------
Show several reports on the same page.

The sample uses two C1WebReport controls in a table. Each control shows
a different report, and both are Ajax-enabled so paging through each 
report doesn't cause flicker.